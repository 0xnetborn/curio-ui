import React from "react";

const WetPaintButton = () => {
  return (
    <button className="px-4 py-2 bg-accent text-white rounded-lg font-semibold relative overflow-hidden">
      Wet Paint
    </button>
  );
};

export default WetPaintButton;
